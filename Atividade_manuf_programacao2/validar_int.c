#include <stdio.h>
#include <string.h>

int main(){
    int num;

    printf("Insira alguma palavra: ");
    

    if(nome[0] == '\n'){
        printf("Dado inválido");
    }else{
        printf("Voce digitou:%s", nome);
    }
    return 0;
}